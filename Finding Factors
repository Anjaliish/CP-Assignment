 Write a C program that finds all the factors of a given number and prints them. As an example, factors of 15 are - 1, 3, 5 and 15#include <stdio.h>

int main()
{
    int num , f;
    
    printf("Enter a positive number:");
    scanf("%d",&num);
    
    printf("Factor of %d are:", num);
    for (f=1; f<=num ;++f)
    {
        if (num % f==0)
        {
            printf("%d\t",f);
        }
    }

    return 0;
}

